<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorAccent">@color/black</item>
        <item name="actionBarTabBarStyle">@style/LineRemover</item>
    </style>

    <style name="LineRemover" parent="@style/Widget.AppCompat.Light.ActionBar.TabView">
        <!-- This empty style overrides standard style. This removs the Line between the tabs -->
    </style>
</resources>
